import React from "react";
import styled from "styled-components";

const ProgressBar = () => {
  return <ProgressBarContainer>ProgressBar</ProgressBarContainer>;
};

export default ProgressBar;

const ProgressBarContainer = styled.div`
  display: flex;
  z-index: 1;
`;
